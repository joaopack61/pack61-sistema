# ============================================================
#  Pack61 - Variaveis de Ambiente (MODELO)
#  No Railway: adicione cada linha abaixo como variavel de ambiente
# ============================================================

# Chave secreta para login (coloque qualquer frase longa e secreta)
JWT_SECRET=troque_por_uma_frase_muito_secreta_aqui_2024

# Caminho do banco de dados SQLite no volume do Railway
DB_PATH=/data/pack61.db

# Caminho da pasta de fotos e canhotos no volume do Railway
UPLOAD_PATH=/data/uploads

# Ambiente de producao
NODE_ENV=production
